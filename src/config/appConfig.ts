/** Name of the Drive folder this app creates for the signed-in user. */
export const DRIVE_FOLDER_NAME = 'Project X'

/** Name of the Google Sheet (inside DRIVE_FOLDER_NAME) that logs each upload. */
export const SHEET_NAME = 'Project X Photo Log'
